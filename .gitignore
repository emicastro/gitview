# Toolbox harness (tb init / handoff). Product docs stay tracked.
toolbox.toml
AGENTS.md
docs/session.md
docs/adr/0000-template.md

# Secrets (never commit; gitview reads GITHUB_TOKEN from the environment)
.env
.env.local
gitview
